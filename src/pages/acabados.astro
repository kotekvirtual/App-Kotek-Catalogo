---
import Layout from "../layouts/Layout.astro";

---


<Layout title="Acabados">
    <main>
            <header class="page-header">
                <h1 class="header-title">Acabados</h1>
                <div class="filter-tabs">
                    <button class="filter-tab" data-filter="pavimento">Pavimento</button>
                    <button class="filter-tab" data-filter="cocina">Cocina</button>
                    <button class="filter-tab" data-filter="baños">Baños</button>
                </div>

                <!-- Pavimento Sub-buttons -->
                <div class="pavimento-sub-buttons hidden" id="pavimento-sub-buttons">
                    <button class="sub-button active" data-pavimento="1">
                        <img src="/boton-1.webp" alt="Pavimento Option 1" />
                    </button>
                    <button class="sub-button" data-pavimento="2">
                        <img src="/boton-2.webp" alt="Pavimento Option 2" />
                    </button>
                    <button class="sub-button" data-pavimento="3">
                        <img src="/boton-3.webp" alt="Pavimento Option 3" />
                    </button>
                </div>

                <!-- Cocina Sub-buttons -->
                <div class="cocina-sub-buttons hidden" id="cocina-sub-buttons">
                    <button class="sub-button active" data-cocina="1">
                        <img src="/boton-1.webp" alt="Cocina Option 1" />
                    </button>
                    <button class="sub-button" data-cocina="2">
                        <img src="/boton-2.webp" alt="Cocina Option 2" />
                    </button>
                    <button class="sub-button" data-cocina="3">
                        <img src="/boton-3.webp" alt="Cocina Option 3" />
                    </button>
                </div>

                <!-- Baños Sub-buttons -->
                <div class="banos-sub-buttons hidden" id="banos-sub-buttons">
                    <button class="sub-button active" data-banos="1">
                        <img src="/boton-1.webp" alt="Baños Option 1" />
                    </button>
                    <button class="sub-button" data-banos="3">
                        <img src="/boton-3.webp" alt="Baños Option 3" />
                    </button>
                </div>
            </header>

            <section class="gallery" id="gallery">
                <div class="gallery-item" data-category="pavimento" data-pavimento="1">
                    <img src="/pavimento-1-1.webp"/>
                </div>
                <div class="gallery-item" data-category="pavimento" data-pavimento="2">
                    <img src="/pavimento-2-1.webp"/>
                </div>
                <div class="gallery-item" data-category="pavimento" data-pavimento="3">
                    <img src="/pavimento-3-1.webp"/>
                </div>
                                        

                <div class="gallery-item" data-category="cocina" data-cocina="1">
                    <img src="/cocina-1.webp"/>
                </div>
                <div class="gallery-item" data-category="cocina" data-cocina="2">
                    <img src="/cocina-2.webp"/>
                </div>
                <div class="gallery-item" data-category="cocina" data-cocina="3">
                    <img src="/cocina-3.webp"/>
                </div>
                
                <div class="gallery-item" data-category="baños" data-banos="1">
                    <img src="/baños-1-1.webp"/>
                </div>
                <div class="gallery-item" data-category="baños" data-banos="3">
                    <img src="/baños-3-1.webp"/>
                </div>



            </section>

            <section class="mobile-gallery" id="mobile-gallery">

                <div class="gallery-item" data-category="pavimento" data-pavimento="1">
                    <img src="/pavimento-1-1.webp"/>
                </div>
                <div class="gallery-item" data-category="pavimento" data-pavimento="2">
                    <img src="/pavimento-2-1.webp"/>
                </div>
                <div class="gallery-item" data-category="pavimento" data-pavimento="3">
                    <img src="/pavimento-3-1.webp"/>
                </div>
                                        

                <div class="gallery-item" data-category="cocina" data-cocina="1">
                    <img src="/cocina-1.webp"/>
                </div>
                <div class="gallery-item" data-category="cocina" data-cocina="2">
                    <img src="/cocina-2.webp"/>
                </div>
                <div class="gallery-item" data-category="cocina" data-cocina="3">
                    <img src="/cocina-3.webp"/>
                </div>
                
                <div class="gallery-item" data-category="baños" data-banos="1">
                    <img src="/baños-1-1.webp"/>
                </div>
                <div class="gallery-item" data-category="baños" data-banos="3">
                    <img src="/baños-3-1.webp"/>
                </div>

            </section>
    </main>
</Layout>



<style>
body {
    overflow: hidden;
}

main {
    position: fixed;
    top: 0;
    left: 280px;
    width: calc(100vw - 280px);
    height: 100vh;
    overflow: hidden;
    background-color: hsl(240, 15%, 17%);
    padding: 0;
}

.header-title{
    font-size: 2rem;
    font-weight: 700;
    color: white;
    margin: 0 0 0px 0;
    padding: 0;
    text-align: center;        
}

.content-container {
    padding: 40px;
    max-width: 1200px;
    width: 100%;
    margin: 0 auto;
}

.page-header {
    position: fixed;
    /*top: 20px;*/
    left: 280px;
    right: 0;
    z-index: 10;
    text-align: center;
    padding: 20px;
    background: transparent;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
}

.filter-tabs {
    display: flex;
    gap: 8px;
    justify-content: center;
}

.pavimento-sub-buttons {
    display: flex;
    gap: 16px;
    justify-content: center;
    margin-top: 16px;
}

.pavimento-sub-buttons.hidden {
    display: none;
}

.cocina-sub-buttons {
    display: flex;
    gap: 16px;
    justify-content: center;
    margin-top: 16px;
}

.cocina-sub-buttons.hidden {
    display: none;
}

.banos-sub-buttons {
    display: flex;
    gap: 16px;
    justify-content: center;
    margin-top: 16px;
}

.banos-sub-buttons.hidden {
    display: none;
}

.sub-button {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    border: 3px solid transparent;
    background: none;
    cursor: pointer;
    transition: all 0.3s ease;
    padding: 0;
    overflow: hidden;
}

.sub-button.active {
    border-color: hsl(230, 100%, 69%);
}

.sub-button:hover {
    border-color: hsl(230, 100%, 75%);
    transform: scale(1.05);
}

.sub-button img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 50%;
}

.filter-tab {
    padding: 12px 24px;
    border: none;
    border-radius: 4px;
    background-color: hsl(240, 15%, 30%);
    color: white;
    font-size: 0.95rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.3s ease;
}

.filter-tab:hover {
    background-color: hsl(240, 15%, 35%);
}

.filter-tab.active {
    background-color: hsl(230, 100%, 69%);
    color: white;
}

.gallery {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
}

.gallery-item {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border-radius: 0;
    overflow: hidden;
}

.gallery-item.hidden {
    display: none;
}

.gallery-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center center;
    display: block;
    border-radius: 0;
    -webkit-touch-callout: none;
    -webkit-user-select: none;
    -khtml-user-select: none;
    -moz-user-select: none;
    -ms-user-select: none;
}

.mobile-gallery {
    display: none;
}

@media (orientation: portrait) {
    .content-container {
        padding: 0;
        max-width: none;
        margin: 0;
    }

    .header-title{
        display: none;     
    }
    
    main {
        position: relative;
        top: auto;
        left: auto;
        width: 100vw;
        height: calc(100dvh - 80px);
        overflow: auto;
        padding: 0;
        display: flex;
        flex-direction: column;
    }

    .page-header {
        position: relative;
        top: auto;
        left: auto;
        right: auto;
        z-index: auto;
        padding: 16px;
        margin-bottom: 0;
        flex-shrink: 0;
        background-color: hsl(240, 15%, 17%);
        width: 100%;
        box-sizing: border-box;
    }

    .filter-tabs {
        justify-content: center;
        overflow-x: none;
        padding-bottom: 8px;
        gap: 8px;
    }

    .filter-tab {
        padding: 8px 16px;
        font-size: 0.9rem;
        white-space: nowrap;
        flex-shrink: 0;
    }

    .pavimento-sub-buttons,
    .cocina-sub-buttons,
    .banos-sub-buttons {
        margin-top: 12px;
        gap: 12px;
    }

    .sub-button {
        width: 50px;
        height: 50px;
    }

    .gallery {
        flex: 1;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        justify-content: flex-start;
        padding: 4px 16px 8px 16px;
        position: relative;
        width: 100%;
        box-sizing: border-box;
        overflow: hidden;
    }

    .gallery-item {
        width: 100%;
        height: 50vh;
        border-radius: 12px;
        overflow: hidden;
        position: relative;
        box-sizing: border-box;
        margin: 0;
    }

    .gallery-item img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        object-position: center center;
        border-radius: 12px;
        display: block;
    }

    .mobile-gallery {
        display: none;
    }

    /* Special handling for baños - show multiple images in scroll */
    .gallery.banos-mobile {
        justify-content: flex-start;
        overflow-y: auto;
        gap: 16px;
        padding: 8px 16px;
    }

    .gallery.banos-mobile .gallery-item {
        flex-shrink: 0;
        margin-bottom: 0;
        height: 60vh;
        max-height: none;
    }

    .gallery.banos-mobile .gallery-item img {
        height: 100%;
        object-fit: cover;
        max-height: none;
    }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const filterTabs = document.querySelectorAll('.filter-tab');
    const galleryItems = document.querySelectorAll('.gallery-item');
    const pavimentoSubButtons = document.getElementById('pavimento-sub-buttons');
    const cocinaSubButtons = document.getElementById('cocina-sub-buttons');
    const banosSubButtons = document.getElementById('banos-sub-buttons');
    const pavimentoButtons = document.querySelectorAll('#pavimento-sub-buttons .sub-button');
    const cocinaButtons = document.querySelectorAll('#cocina-sub-buttons .sub-button');
    const banosButtons = document.querySelectorAll('#banos-sub-buttons .sub-button');

    function filterGallery(category) {
        galleryItems.forEach(item => {
            if (item.dataset.category === category) {
                item.classList.remove('hidden');
                item.style.display = 'block';
            } else {
                item.classList.add('hidden');
                item.style.display = 'none';
            }
        });
    }

    function filterPavimentoImages(pavimentoType) {
        // Hide all gallery items first
        galleryItems.forEach(item => {
            item.classList.add('hidden');
            item.style.display = 'none';
        });

        // Show only the specific pavimento image
        const targetPavimentoItem = document.querySelector(`.gallery-item[data-category="pavimento"][data-pavimento="${pavimentoType}"]`);
        if (targetPavimentoItem) {
            targetPavimentoItem.classList.remove('hidden');
            targetPavimentoItem.style.display = 'block';
        }
    }

    function filterCocinaImages(cocinaType) {
        // Hide all gallery items first
        galleryItems.forEach(item => {
            item.classList.add('hidden');
            item.style.display = 'none';
        });

        // Show only the specific cocina image
        const targetCocinaItem = document.querySelector(`.gallery-item[data-category="cocina"][data-cocina="${cocinaType}"]`);
        if (targetCocinaItem) {
            targetCocinaItem.classList.remove('hidden');
            targetCocinaItem.style.display = 'block';
        }
    }

    function filterBanosImages(banosType) {
        // Hide all gallery items first
        galleryItems.forEach(item => {
            item.classList.add('hidden');
            item.style.display = 'none';
        });

        // Show only the specific baños image
        const targetBanosItem = document.querySelector(`.gallery-item[data-category="baños"][data-banos="${banosType}"]`);
        if (targetBanosItem) {
            targetBanosItem.classList.remove('hidden');
            targetBanosItem.style.display = 'block';
        }
    }

    function updateActiveTab(activeTab) {
        filterTabs.forEach(tab => {
            tab.classList.remove('active');
        });
        activeTab.classList.add('active');
    }

    function updateActivePavimentoButton(activeButton) {
        pavimentoButtons.forEach(button => {
            button.classList.remove('active');
        });
        activeButton.classList.add('active');
    }

    function updateActiveCocinaButton(activeButton) {
        cocinaButtons.forEach(button => {
            button.classList.remove('active');
        });
        activeButton.classList.add('active');
    }

    function updateActiveBanosButton(activeButton) {
        banosButtons.forEach(button => {
            button.classList.remove('active');
        });
        activeButton.classList.add('active');
    }

    filterTabs.forEach(tab => {
        tab.addEventListener('click', function() {
            const filter = this.dataset.filter;
            updateActiveTab(this);

            if (filter === 'pavimento') {
                // Hide cocina sub-buttons and show pavimento sub-buttons
                cocinaSubButtons.classList.add('hidden');
                pavimentoSubButtons.classList.remove('hidden');
                // Show only first pavimento image by default
                filterPavimentoImages('1');
                // Activate first pavimento sub-button
                if (pavimentoButtons.length > 0) {
                    updateActivePavimentoButton(pavimentoButtons[0]);
                }
            } else if (filter === 'cocina') {
                // Hide pavimento sub-buttons and show cocina sub-buttons
                pavimentoSubButtons.classList.add('hidden');
                cocinaSubButtons.classList.remove('hidden');
                // Show only first cocina image by default
                filterCocinaImages('1');
                // Activate first cocina sub-button
                if (cocinaButtons.length > 0) {
                    updateActiveCocinaButton(cocinaButtons[0]);
                }
            } else if (filter === 'baños') {
                // Hide pavimento and cocina sub-buttons, show baños sub-buttons
                pavimentoSubButtons.classList.add('hidden');
                cocinaSubButtons.classList.add('hidden');
                banosSubButtons.classList.remove('hidden');
                // Remove special class for mobile layout since we now use sub-buttons
                const gallery = document.getElementById('gallery');
                gallery.classList.remove('banos-mobile');
                // Show only first baños image by default
                filterBanosImages('1');
                // Activate first baños sub-button
                if (banosButtons.length > 0) {
                    updateActiveBanosButton(banosButtons[0]);
                }
            } else {
                // Hide all sub-buttons
                pavimentoSubButtons.classList.add('hidden');
                cocinaSubButtons.classList.add('hidden');
                banosSubButtons.classList.add('hidden');
                // Remove special class
                const gallery = document.getElementById('gallery');
                gallery.classList.remove('banos-mobile');
                // Show regular filter
                filterGallery(filter);
            }
        });
    });

    // Handle pavimento sub-buttons
    pavimentoButtons.forEach(button => {
        button.addEventListener('click', function() {
            const pavimentoType = this.dataset.pavimento;
            updateActivePavimentoButton(this);
            filterPavimentoImages(pavimentoType);
        });
    });

    // Handle cocina sub-buttons
    cocinaButtons.forEach(button => {
        button.addEventListener('click', function() {
            const cocinaType = this.dataset.cocina;
            updateActiveCocinaButton(this);
            filterCocinaImages(cocinaType);
        });
    });

    // Handle baños sub-buttons
    banosButtons.forEach(button => {
        button.addEventListener('click', function() {
            const banosType = this.dataset.banos;
            updateActiveBanosButton(this);
            filterBanosImages(banosType);
        });
    });

    // Initialize with "Pavimento" selected
    const pavimentoTab = document.querySelector('[data-filter="pavimento"]');
    if (pavimentoTab) {
        pavimentoTab.classList.add('active');
        pavimentoTab.click();
    }
});
</script>
