---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Residencial Demo - Descubre">
    <main class="video-landing-main">
        <div class="video-container">
            <video autoplay muted playsinline class="background-video">
                <source src="/video_landing_2.mp4" type="video/mp4">
                Su navegador no soporta el elemento de video.
            </video>
        </div>

        <div class="content-overlay">
            <div class="navigation-buttons">
                <button class="nav-button" id="urbanizacionBtn">
                    <div class="nav-button-icon">
                        <svg width="48" height="48" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
                            <path d="M11,15h-2v-2h2v2Zm4-2h-2v2h2v-2Zm-4,4h-2v2h2v-2Zm4,0h-2v2h2v-2ZM11,5h-2v2h2v-2Zm4,0h-2v2h2v-2Zm-4,4h-2v2h2v-2Zm4,0h-2v2h2v-2Zm9-1c0-1.654-1.346-3-3-3h-2V3c0-1.654-1.346-3-3-3H8c-1.654,0-3,1.346-3,3v6H3c-1.654,0-3,1.346-3,3v12H24V8ZM2,12c0-.552,.449-1,1-1H7V3c0-.552,.449-1,1-1h8c.551,0,1,.448,1,1V7h4c.551,0,1,.448,1,1v14H2V12Zm18,1h-2v2h2v-2Zm0,4h-2v2h2v-2ZM6,13h-2v2h2v-2Zm0,4h-2v2h2v-2Zm14-8h-2v2h2v-2Z"/>
                        </svg>
                    </div>
                    <span>Edificio</span>
                </button>

                <button class="nav-button" id="acabadosBtn">
                    <div class="nav-button-icon">
                        <svg width="48" height="48" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
                            <g id="_01_align_center" data-name="01 align center"><path d="M21,0H3A3,3,0,0,0,0,3V24H24V3A3,3,0,0,0,21,0ZM3,2H21a1,1,0,0,1,1,1V20.586L11.121,9.707a3,3,0,0,0-4.242,0L2,14.586V3A1,1,0,0,1,3,2ZM2,17.414l6.293-6.293a1,1,0,0,1,1.414,0L20.586,22H2Z"/><path d="M16,10a3,3,0,1,0-3-3A3,3,0,0,0,16,10Zm0-4a1,1,0,1,1-1,1A1,1,0,0,1,16,6Z"/></g>
                        </svg>
                    </div>
                    <span>Acabados</span>
                </button>

                <button class="nav-button" id="ubicacionBtn">
                    <div class="nav-button-icon">
                        <svg width="48" height="48" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
                            <path d="m16.242,1.757c-1.134-1.133-2.641-1.757-4.242-1.757s-3.11.624-4.242,1.757c-1.134,1.133-1.758,2.64-1.758,4.243s.624,3.11,1.77,4.254l4.23,4.138,4.242-4.149c1.134-1.133,1.758-2.64,1.758-4.243s-.624-3.11-1.758-4.243Zm-4.242,6.243c-1.105,0-2-.895-2-2s.895-2,2-2,2,.895,2,2-.895,2-2,2Zm12,7.701v1.099l-12,7.2L0,16.8v-1.099l6.494-3.897,2.217,2.169-3.796,2.277,7.084,4.25,7.084-4.25-3.796-2.277,2.217-2.168,6.494,3.896Z"/>
                        </svg>
                    </div>
                    <span>Ubicación</span>
                </button>
            </div>
        </div>
    </main>
</Layout>

<style>
    .video-landing-main {
        position: relative;
        width: 100vw;
        height: 100vh;
        overflow: hidden;
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: #000;
    }

    .video-container {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 1;
    }

    .background-video {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .content-overlay {
        position: relative;
        z-index: 2;
        text-align: center;
        color: white;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100%;
        background: rgba(0, 0, 0, 0.3);
        width: 100%;
    }

    .navigation-buttons {
        display: flex;
        gap: 2rem;
        justify-content: center;
        align-items: center;
        flex-wrap: wrap;
        margin-top: -10vh;
    }

    .nav-button {
        background: rgba(255, 255, 255, 0.1);
        border: 4px solid rgba(255, 255, 255, 0.8);
        border-radius: 20px;
        padding: 2.5rem 2rem;
        color: white;
        font-family: "Poppins", sans-serif;
        font-size: 1.4rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1.2rem;
        min-width: 200px;
        text-transform: uppercase;
        letter-spacing: 1.5px;
        backdrop-filter: blur(15px);
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        position: relative;
        overflow: hidden;
    }

    .nav-button::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(
            90deg,
            transparent,
            rgba(255, 255, 255, 0.1),
            transparent
        );
        transition: left 0.6s ease;
    }

    .nav-button:hover::before {
        left: 100%;
    }

    .nav-button:hover {
        background: rgba(255, 255, 255, 0.2);
        border-color: rgba(255, 255, 255, 1);
        transform: translateY(-6px) scale(1.02);
        box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
        backdrop-filter: blur(20px);
    }

    .nav-button:active {
        transform: translateY(-2px);
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
    }

    .nav-button-icon {
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .nav-button-icon svg {
        filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.3));
    }

    /* Responsive design */
    @media (max-width: 1000px) and (orientation: portrait) {
        .navigation-buttons {
            flex-direction: column;
            gap: 1.5rem;
        }

        .nav-button {
            min-width: 200px;
            padding: 1.5rem 1rem;
            font-size: 1rem;
        }

        .nav-button-icon svg {
            width: 40px;
            height: 40px;
        }
    }

    @media (max-width: 1000px) and (orientation: landscape) {
        .navigation-buttons {
            flex-direction: row;
            gap: 1rem;
            margin-top: -5vh;
        }

        .nav-button {
            min-width: 140px;
            padding: 1rem 0.8rem;
            font-size: 0.8rem;
            gap: 0.8rem;
        }

        .nav-button-icon svg {
            width: 32px;
            height: 32px;
        }
    }

    @media (max-width: 480px) and (orientation: portrait) {
        .navigation-buttons {
            gap: 1rem;
            padding: 0 1rem;
            flex-direction: column;
        }

        .nav-button {
            min-width: 160px;
            padding: 1.2rem 0.8rem;
            font-size: 0.9rem;
        }

        .nav-button-icon svg {
            width: 36px;
            height: 36px;
        }
    }

    @media (max-width: 480px) and (orientation: landscape) {
        .navigation-buttons {
            flex-direction: row;
            gap: 0.8rem;
            padding: 0 0.5rem;
            margin-top: -3vh;
        }

        .nav-button {
            min-width: 100px;
            padding: 0.8rem 0.5rem;
            font-size: 0.7rem;
            gap: 0.5rem;
        }

        .nav-button-icon svg {
            width: 28px;
            height: 28px;
        }
    }
</style>

<script type="module">
    document.addEventListener('DOMContentLoaded', function() {
        const urbanizacionBtn = document.getElementById('urbanizacionBtn');
        const acabadosBtn = document.getElementById('acabadosBtn');
        const ubicacionBtn = document.getElementById('ubicacionBtn');

        urbanizacionBtn.addEventListener('click', () => {
            window.location.href = '/main-landing';
        });

        acabadosBtn.addEventListener('click', () => {
            window.location.href = '/acabados';
        });

        ubicacionBtn.addEventListener('click', () => {
            window.location.href = '/ubicacion';
        });
    });
</script>